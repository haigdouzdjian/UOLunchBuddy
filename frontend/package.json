{
  "name": "uo-lunch-buddy",
  "version": "0.0.1",
  "scripts": {
    "start": "node -r dotenv/config index.js",
    "test": "standard --fix && node -r dotenv/config index.js",
    "lint": "standard --fix",
    "build": "yarn install --frozen-lockfile"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cookie-parser": "^1.4.4",
    "express": "^4.17.1",
    "multer": "^1.4.2",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "prettier": "^1.19.1",
    "standard": "^14.3.1"
  }
}
